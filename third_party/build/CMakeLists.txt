set(THIRDPARTY_SUBDIRECTORIES resample srtp)

foreach(SUBDIRECTORY ${THIRDPARTY_SUBDIRECTORIES})
  add_subdirectory(${SUBDIRECTORY})
  set(THIRDPARTY_OBJECTS_LIST ${THIRDPARTY_OBJECTS_LIST} $<TARGET_OBJECTS:${SUBDIRECTORY}>)
endforeach()
